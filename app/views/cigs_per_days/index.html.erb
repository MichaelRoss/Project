<h1>Listing cigs_per_days</h1>

<table>
  <tr>
    <th>No of cigs</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cigs_per_days.each do |cigs_per_day| %>
  <tr>
    <td><%= cigs_per_day.no_of_cigs %></td>
    <td><%= link_to 'Show', cigs_per_day %></td>
    <td><%= link_to 'Edit', edit_cigs_per_day_path(cigs_per_day) %></td>
    <td><%= link_to 'Destroy', cigs_per_day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cigs per day', new_cigs_per_day_path %>
